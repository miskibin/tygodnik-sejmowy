// Thin re-export shim — the real implementation lives in the shared
// editorial atom kit so /tygodnik can use the same chrome. Kept so
// existing imports inside this directory continue to work.

export { SectionHead, Kicker } from "@/components/tygodnik/atoms/SectionHead";

Review five supplied images of one candidate. Image 1 is the full frame; images 2–5 are detailed quadrants in reading order: top-left, top-right, bottom-left, bottom-right. Return exactly one flat JSON object matching the schema, never a wrapper or Markdown.

Inspect every quadrant for small readable lettering, graffiti, logos, people/faces and physical errors. Any visible lettering, including distant graffiti, requires `rejected`. Check semantic relevance, source identity where required, and geometry. Normal photographic grain or ordinary camera noise is not a synthetic artifact and AI provenance alone is not a visual defect.

Use `approved` only when every check passes. Use `needs_review` when uncertain and `rejected` for visible violations. Give a concrete Polish reason.

Example shape only:
{"status":"rejected","reason":"W prawej dolnej ćwiartce widać czytelne graffiti.","semantic_relevance":true,"physical_geometry_ok":true,"has_people_or_faces":false,"has_text_or_logos":true,"has_synthetic_artifacts":false,"identity_supported":true}
